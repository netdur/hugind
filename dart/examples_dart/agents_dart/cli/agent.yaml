name: "cli"
version: "0.2.0"
description: "A context-aware terminal assistant that translates natural language to shell commands."
hugind_version: ">=0.6.0"
entry_point: "main.dart"
backend: "qwen3vl-8b"

permissions:
  network:
    allow: false
  filesystem:
    read: false
    write: false
  shell:
    allow: true

dependencies:
  mcp: []

env: []