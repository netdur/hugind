name: "port-sheriff"
version: "0.1.0"
description: "Finds and terminates the process bound to a port using available CLI tools."
hugind_version: ">=0.6.0"
entry_point: "main.drt"
backend: "gemma-4b"

permissions:
  network:
    allow: false
  filesystem:
    read: false
    write: false
  shell:
    allow: true

dependencies:
  mcp: []

env: []
