name: "JokeBot"
version: "0.1.0"
description: "A friendly bot that tells jokes."
hugind_version: ">=0.6.0"
entry_point: "main.drt"
backend: "gemma-4b"

permissions:
  network:
    allow: false
  filesystem:
    read: false
    write: false
  shell:
    allow: false

dependencies:
  mcp: []

env: []
