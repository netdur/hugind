name: "unix-cli-tutor"
version: "1.0.0"
description: "Translates plain English requests into safe shell commands and executes them upon confirmation."
author: "hugind"
license: "MIT"
url: "https://github.com/hugind/hugind-agents"
hugind_version: ">=0.6.0"

entry_point: "main.dart"
backend: "qwen3vl-8b"

permissions:
  network:
    allow: false
  filesystem:
    read: true
    write: false
  shell:
    allow: true
    whitelist:
      - "sh"

dependencies:
  mcp: []

env: []
